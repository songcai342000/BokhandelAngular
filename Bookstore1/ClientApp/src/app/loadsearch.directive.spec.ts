import { LoadsearchDirective } from './loadsearch.directive';

describe('LoadsearchDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadsearchDirective();
    expect(directive).toBeTruthy();
  });
});
