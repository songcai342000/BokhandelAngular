import { LoadcartDirective } from './loadcart.directive';

describe('LoadcartDirective', () => {
  it('should create an instance', () => {
    const directive = LoadcartDirective;
    expect(directive).toBeTruthy();
  });
});
