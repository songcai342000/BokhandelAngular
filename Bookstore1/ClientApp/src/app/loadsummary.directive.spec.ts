import { LoadsummaryDirective } from './loadsummary.directive';

describe('LoadsummaryDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadsummaryDirective();
    expect(directive).toBeTruthy();
  });
});
